{
  "name": "note-taker",
  "version": "1.0.0",
  "description": "note taking application",
    "engines": {
    "node": "6.11.1"
    }
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js"
  }
}
